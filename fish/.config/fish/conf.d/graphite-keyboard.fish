# Some of the keybinds do not work very well because of funny keyboard layout,
# potentially without alt key (IBM model M).
# So, remap!

bind \ce edit_command_buffer
bind -M insert \ce edit_command_buffer
